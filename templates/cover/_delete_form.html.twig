<form method="post" action="{{ path('app_cover_delete', {'id': cover.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cover.id) }}">
    <button class="btn">Delete</button>
</form>
